# 🔬 ДИАГНОСТИКА ПРОБЛЕМ С API СДЭК

## 🎯 ТЕКУЩАЯ СИТУАЦИЯ:

✅ **Габариты товаров читаются корректно:**
```
✅ Найдены габариты из WC блока товара: {length: 10, width: 10, height: 10, quantity: 1}
✅ Найден вес из WC блока товара: 80 г, количество: 1
```

❌ **НО API возвращает ошибку:**
```
❌ API СДЭК вернул ошибку: API СДЭК недоступен, расчет стоимости невозможен
🔍 Данные для отладки: {point_code: 'UFA134', cart_weight: 80, cart_value: 180180, cart_dimensions: {length: 11, width: 11, height: 11}}
```

## 🛠️ КАК НАЙТИ ПРИЧИНУ:

### 1. 📊 Проверьте логи сервера

**Откройте файл `check_logs.php` в браузере** для просмотра логов СДЭК:
```
http://ваш-сайт.ru/check_logs.php
```

### 2. 🔍 Ищите в логах следующие этапы:

#### ЭТАП 1: Авторизация (🔑)
```
🔑 СДЭК AUTH: Получаем новый токен авторизации
🔑 СДЭК AUTH: ✅ Токен получен успешно
```
**Если ошибка здесь** → проблема с учетными данными API

#### ЭТАП 2: Определение города (🏙️)
```  
🏙️ СДЭК API: Найден город Уфа (код: 414) по префиксу пункта: UFA
```
**Если ошибка здесь** → проблема с определением локации

#### ЭТАП 3: Запрос к API (🚀📤)
```
🚀 СДЭК API: Отправляем запрос к https://api.cdek.ru/v2/calculator/tariff
📤 СДЭК API: Данные запроса: {JSON данные}
```
**Если ошибка здесь** → проблема с формированием запроса

#### ЭТАП 4: Ответ от API (📥)
```
📥 СДЭК API: HTTP код ответа: 200
🎉 СДЭК API: Успешно получена стоимость от API: 4210 руб.
```
**Если ошибка здесь** → проблема с ответом API

## 🧪 ВОЗМОЖНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ:

### 1. ❌ Ошибка авторизации
**Симптомы:** 
```
🔑 СДЭК AUTH: ❌ Не удалось получить токен
```
**Решение:** Проверьте настройки API в админ-панели WordPress

### 2. ❌ Ошибка определения города
**Симптомы:**
```
⚠️ СДЭК API: Код пункта "UFA134" не найден в списке городов
```
**Решение:** Код UFA есть в списке, но проверьте логи

### 3. ❌ Ошибка HTTP запроса
**Симптомы:**
```  
❌ СДЭК API: Некорректный ответ. HTTP код: 400
```
**Решение:** Проверьте данные запроса (габариты, вес)

### 4. ❌ Ошибка в данных API
**Симптомы:**
```
❌ СДЭК API: API вернул ошибки: [массив ошибок]
```
**Решение:** Смотрите конкретные ошибки API

## 🔧 БЫСТРЫЕ ИСПРАВЛЕНИЯ:

### Если проблема с тестовым/боевым режимом:
```php
// В cdek-delivery-plugin.php, строка ~418:
$this->test_mode = get_option('cdek_test_mode', 0); // 0 = боевой, 1 = тест
```

### Если проблема с габаритами (слишком маленькие):
```php  
// Минимальные размеры в API:
'length' => max(10, intval($cart_dimensions['length'])), // Минимум 10см
'width' => max(10, intval($cart_dimensions['width'])),   // Минимум 10см  
'height' => max(5, intval($cart_dimensions['height']))   // Минимум 5см
```

### Если проблема с весом (слишком легкий):
```php
'weight' => max(100, intval($cart_weight)), // Минимум 100г
```

## 📞 СЛЕДУЮЩИЕ ШАГИ:

1. **Откройте `check_logs.php` в браузере**
2. **Выберите пункт выдачи UFA134 на сайте**  
3. **Обновите страницу логов**
4. **Найдите где именно происходит ошибка**
5. **Пришлите логи с ошибками**

**С подробными логами мы быстро найдем и исправим проблему! 🚀**